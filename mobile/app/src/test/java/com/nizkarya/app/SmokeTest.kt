package com.nizkarya.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder proving the unit-test toolchain works; real tests arrive with
 * the ported business logic (quick-add parser, habit scheduling, streaks).
 */
class SmokeTest {
    @Test
    fun `test infrastructure runs`() {
        assertEquals(4, 2 + 2)
    }
}
